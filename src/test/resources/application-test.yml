spring:
  application:
    name: tango-songs
  datasource:
    url: jdbc:h2:mem:mydb
    username: sa
    password: password
    driverClassName: org.h2.Driver
    jpa:
      spring.jpa.database-platform: org.hibernate.dialect.H2Dialect
  security:
    oauth2:
      client:
        provider:
          spotify:
            token-uri: "https://accounts.spotify.com/api/token"
        registration:
          spotify:
            client-id: 2e6c3c5d45ca4a0799ab1ddc87419987
            client-secret: a6b7b7b4774344fa89f7b6bb06aa8a59
            authorization-grant-type: client_credentials
            client-authentication-method: client_secret_basic

feign:
  client:
    config:
      spotify:
        url: https://api.spotify.com

application:
  scheduling:
    enabled: false